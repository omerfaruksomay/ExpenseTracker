import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function ExpensesScreen({ navigation }) {
    return (
        <View>
            <Text>This is Expenses ExpensesScreen</Text>
        </View>
    )
}

const styles = StyleSheet.create({})